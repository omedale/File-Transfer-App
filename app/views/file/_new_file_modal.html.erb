<div  class="modal fade add-file-modal" id="addNewFile" tabindex="-1" role="dialog" aria-labelledby="exampleModalCenterTitle" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered" role="document">
    <div class="modal-content">
      <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLongTitle">Add File</h5>
        <button type="button" class="close" data-dismiss="modal" aria-label="Close">
          <span aria-hidden="true">&times;</span>
        </button>
      </div>
     <%= form_with(model: @upload, local: true) do |f| %>
      <div class="modal-body">
       <div class="form-group">
         <%= f.text_field :title, required: true, class: "form-control file-name text-center", placeholder: 'File title' %>
        </div>
         <div class="form-group">
           <%= f.text_area :description, required: true, class: "form-control file-name text-center", placeholder: 'File description' %>
        </div>
         <div class="form-group">
           <%= f.password_field :passphrase, required: true, class: "form-control file-name text-center", placeholder: 'Please enter secure passphrase' %>
        </div>
         <div id="progress-wrap" class="form-group">
          <div id="progress-bar"></div>
         </div>
        <div class="form-group">
             <%= f.file_field :files,  required: true, multiple: true, direct_upload: true , class: "dropzone" %>
        </div>
      </div>
      <div class="modal-footer">
         <button type="button" id="reset-form" class="btn btn-secondary">Reset</button>
         <%= f.submit 'UPLOAD', :class => 'btn btn-primary btn-upload', :id=> 'upload-file', data: { disable_with: false } %>
      </div>
     <% end %>
    </div>
  </div>
</div>